# Changelog

## Unreleased — engineering beta

- Replace fixed sleep-based demo behavior with injected asynchronous agent handlers.
- Add bounded workflow, objective and timeout validation.
- Add deterministic workflow/step result models and JSON output.
- Add real tests for sequencing, validation, output handling and timeout behavior.
- Replace echo-only CI with compile, Ruff, pytest, dependency audit, CLI smoke test, Docker build and non-root verification gates.
- Minimize Python dependencies and harden the runtime container.
- Document truthful product scope, SKYCOIN4444 integration boundary and security responsibilities.
